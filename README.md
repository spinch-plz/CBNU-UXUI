# Figma file (virtual)

Pages:

- /test  (2 frames, guid=0:1)

Components:
  0 local → /<page>/components/
  2 page-scoped external → /<page>/external/
  0 shared external → /external-shared/

Tools: ls(path), read_file(path), grep(pattern), pathForId(id), idForPath(path), read_bytes(path)
